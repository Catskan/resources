all:
  children:
    linux_hosts:
      hosts:
        nas.eonelia.fr:
          ansible_connection: ssh
          ansible_host: 100.64.0.6
          ansible_port: 3434
          ansible_user: a.busutil
        arch-linux-laptop:
    macos_hosts:
      hosts:
        macbook-m2:
        macbook-air-aurelien:
    windows_hosts:
      hosts:
        w11-vm-aurel:
        aurelien-gaming:
    proxmox_hosts:
      children:
        proxmox_wyse:
          hosts:
            wyse-proxmox:
        proxmox_optiplex:
          hosts:
            optiplex-proxmox:
    immich_hosts:
      hosts:
        immich:
    caddy_hosts:
      hosts:
        caddy:
    claude_code_hosts:
      hosts:
        claude-code:
    headscale_hosts:
      hosts:
        headscale:
