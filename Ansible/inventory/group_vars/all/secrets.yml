---
# Volontairement vide : tous les secrets communs sont désormais résolus à la
# demande via lookup('viczem.keepass.keepass', ...) dans les fichiers
# host_vars/<host>/secrets.yml et connection.yml. Ce fichier reste comme
# emplacement conventionnel (group_vars/all) si un secret réellement commun
# à TOUS les hôtes devait un jour être ajouté.
