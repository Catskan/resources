# Mot de passe sudo du M2 (compte abusutil) — résolu depuis KeePass à l'exécution.
# Entrée : `Local/Macbook Pro` dans Aurel-vault.kdbx (champ password).
ansible_become_password: "{{ lookup('viczem.keepass.keepass', 'Local/Macbook Pro', 'password') }}"
