# Mot de passe sudo du M2 (compte abusutil) — résolu depuis KeePass à l'exécution.
# À CRÉER dans Aurel-vault.kdbx : entrée `ansible/macbook-m2/sudo`
# (le champ password = le mot de passe sudo du M2).
ansible_become_password: "{{ lookup('viczem.keepass.keepass', 'ansible/macbook-m2/sudo', 'password') }}"
